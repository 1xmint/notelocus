Sample notes used by the `fixtures` check.

That check is path-filtered: it runs only when something here changes, and skips
otherwise. A skipped check is a conclusion GitLocus handles in code and has never
seen in practice, which is one of the two things this repository is measuring.
